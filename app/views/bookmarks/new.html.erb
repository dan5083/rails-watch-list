<h1>Add a Bookmark</h1>

<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :movie, collection: Movie.all, label_method: :title %>
  <%= f.input :comment %>
  <%= f.button :submit %>
<% end %>
